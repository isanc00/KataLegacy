# Configuración dev 
spring:
  application:
    name: legacy-migration-engine
  profiles:
    active: dev
app: 
  cors:
    allowed-origins: "http://localhost:4200"
---

---
# Perfil prod
spring:
  config:
    activate:
      on-profile: prod
springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false
app: 
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS}
